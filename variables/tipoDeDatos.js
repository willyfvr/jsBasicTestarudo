// Tipos de Datos

// String (Cadena de texto)
let nombre = "Juan";

// Number (Números)
let edad = 25;

let altura = 1.75;

// Boolean (Booleano)
let esEstudiante = true;

// Undefined (Indefinido)
let direccion; // No se le ha asignado un valor aún

// Null (Nulo)
let telefono = null; // Intencionalmente sin valor

// Object (Objeto)
let persona = {
  nombre: "Ana",
  edad: 28,
  esEstudiante: false
};

// Array (Arreglo)
let colores = ["rojo", "verde", "azul"];

// Mostrar los valores y sus tipos
console.log("Nombre:", nombre, "| Tipo:", typeof nombre);
console.log("Edad:", edad, "| Tipo:", typeof edad);
console.log("Altura:", altura, "| Tipo:", typeof altura);


